═══════════════════════════════════════════════════════════════
  📱 COMPLETE iOS SHORTCUT FLOW - WITH SENDER EXTRACTION
═══════════════════════════════════════════════════════════════

THE PROBLEM
───────────────────────────────────────────────────────────────

The "Sender" variable isn't directly available from the Message trigger.
You need to EXTRACT it first using "Get Details of Messages" action.


THE COMPLETE FLOW
───────────────────────────────────────────────────────────────

Your automation needs TWO actions in this order:

[Action 1] Get Details of Messages (extracts sender info)
[Action 2] Get Contents of URL (sends to Cloudflare)


STEP-BY-STEP REBUILD
───────────────────────────────────────────────────────────────

PART 1: Clear Current Actions (if needed)
──────────────────────────────────────────

If you already have "Get Contents of URL" action:
1. Tap the (x) or delete button on it
2. Start fresh

Or you can add the new action BEFORE the existing one.


PART 2: Add First Action - Extract Sender
──────────────────────────────────────────

1. Search for: "Get Details of Messages"
2. Add this action

You'll see:
┌────────────────────────────────────────┐
│ Get Details of Messages                │
│                                        │
│ Get [All Details ▼] of [Messages]     │
└────────────────────────────────────────┘

3. Tap "All Details" dropdown
4. Change it to: "Sender"

Result:
┌────────────────────────────────────────┐
│ Get Details of Messages                │
│                                        │
│ Get [Sender ▼] of [Shortcut Input]    │
└────────────────────────────────────────┘

This extracts the sender from the message.


PART 3: Add Second Action - HTTP Request
─────────────────────────────────────────

5. Search for: "Get Contents of URL"
6. Add this action

7. Configure URL:
   https://bank-sms-ledger.abdullah-915.workers.dev/ingest

8. Tap "Show More"

9. Method: Change to "POST"

10. Headers: Add new field → Add Header
    Key: Content-Type
    Value: application/json

11. Request Body: Change to "JSON"


PART 4: Build JSON with 3 Dictionaries
───────────────────────────────────────

Now build the JSON body with all 3 fields:

Dictionary 1: body
──────────────────
Add new field → Dictionary
  Key: body
  Value: Tap and select "Shortcut Input" ← This is the SMS text


Dictionary 2: sender
────────────────────
Add new field → Dictionary
  Key: sender
  Value: Tap and select "Sender" ← This NOW appears because of Action 1!


Dictionary 3: received_at
─────────────────────────
Add new field → Dictionary
  Key: received_at
  Value: Tap and select "Current Date"


FINAL STRUCTURE
───────────────────────────────────────────────────────────────

Your automation should look like:

┌────────────────────────────────────────────────────┐
│ When: Message from "AlRajhiBank"                   │
│                                                    │
│ [1] Get Details of Messages                        │
│     Get Sender of Shortcut Input                   │
│                                                    │
│ [2] Get Contents of URL                            │
│     URL: https://bank-sms-ledger....              │
│     Method: POST                                   │
│     Headers: Content-Type: application/json        │
│     Request Body: JSON                             │
│       Dictionary: body → [Shortcut Input]          │
│       Dictionary: sender → [Sender]                │
│       Dictionary: received_at → [Current Date]     │
└────────────────────────────────────────────────────┘


WHY THIS WORKS
───────────────────────────────────────────────────────────────

Action 1 creates a new variable called "Sender" that contains the
sender information from the message.

Action 2 can now use that "Sender" variable in the JSON body.

Without Action 1, the "Sender" variable doesn't exist!


ALTERNATIVE: SIMPLER VERSION (If Always Same Bank)
───────────────────────────────────────────────────────────────

If you only care about one bank and don't need the dynamic sender:

Skip Action 1 entirely.

In Action 2, for the sender value:
- Instead of selecting a variable
- Select "Text" from "Add new field"
- Type the sender name directly: "AlRajhiBank"

This hardcodes the sender but works fine if you filtered the
trigger to only fire for that specific bank anyway.

JSON would look like:
┌────────────────────────────────────────┐
│ Dictionary: body → [Shortcut Input]    │
│ Dictionary: sender → AlRajhiBank       │  ← Static text
│ Dictionary: received_at → [Current Date]│
└────────────────────────────────────────┘


WHICH APPROACH TO USE?
───────────────────────────────────────────────────────────────

Use DYNAMIC (with Action 1) if:
- You want to track multiple banks
- You want actual sender phone numbers captured
- You want flexibility

Use STATIC (hardcode) if:
- Only tracking one bank
- You filtered trigger by sender anyway
- You want simpler setup


RECOMMENDATION
───────────────────────────────────────────────────────────────

For now, use the STATIC approach since you're testing with one bank:

In sender dictionary:
  Key: sender
  Value: Text → "AlRajhiBank" (or your bank's sender name)

This is simpler and gets you testing faster.

Later (Issue #2+), if you add more banks, you can switch to the
dynamic approach.

═══════════════════════════════════════════════════════════════

═══════════════════════════════════════════════════════════════
  📱 iOS SHORTCUT DETAILED WALKTHROUGH
═══════════════════════════════════════════════════════════════

PART 1: Create the Automation (Steps 1-2)
───────────────────────────────────────────────────────────────

1. Open Shortcuts → Automation → + → Create Personal Automation
2. Select "Message" → Choose "Sender" → Type bank name → "Run Immediately" ON
3. Tap "Next"

Now you're in the Actions screen...


PART 2: Add "Get Contents of URL" Action
───────────────────────────────────────────────────────────────

4. In the search box at bottom, type: "Get Contents of URL"
5. Tap the "Get Contents of URL" result to add it

You'll now see a card that looks like:
┌──────────────────────────────────────┐
│ Get Contents of [URL]                │
│                                      │
│ URL: [empty blue field]              │
└──────────────────────────────────────┘

6. Tap the blue "URL" field and paste:
   https://bank-sms-ledger.abdullah-915.workers.dev/ingest


PART 3: Configure Method (Switch to POST)
───────────────────────────────────────────────────────────────

7. Tap "Show More" (at bottom of the card)

The card expands to show more options:
┌──────────────────────────────────────┐
│ Get Contents of URL                  │
│ URL: https://bank-sms-ledger....     │
│                                      │
│ Method: [GET ▼]  ← TAP THIS          │
│ Headers: [empty]                     │
│ Request Body: [empty]                │
└──────────────────────────────────────┘

8. Tap "Method" dropdown (currently says "GET")
9. Select "POST" from the list


PART 4: Add Headers (Tell server we're sending JSON)
───────────────────────────────────────────────────────────────

Now you should see:
┌──────────────────────────────────────┐
│ Method: POST                         │
│ Headers: [Add new field]  ← TAP THIS │
│ Request Body: [empty]                │
└──────────────────────────────────────┘

10. Tap "Add new field" under Headers
11. You'll see a menu - select "Add Header"

Two text fields appear:
┌──────────────────────────────────────┐
│ Headers:                             │
│   [Key field]                        │
│   [Value field]                      │
└──────────────────────────────────────┘

12. In the KEY field, type: Content-Type
13. In the VALUE field, type: application/json

It should now look like:
┌──────────────────────────────────────┐
│ Headers:                             │
│   Content-Type                       │
│   application/json                   │
└──────────────────────────────────────┘


PART 5: Configure Request Body (The JSON payload)
───────────────────────────────────────────────────────────────

Below Headers, you'll see:
┌──────────────────────────────────────┐
│ Request Body: [File ▼]  ← TAP THIS   │
└──────────────────────────────────────┘

14. Tap the "Request Body" dropdown (currently says "File")
15. Select "JSON" from the list

NOW - This is the important part! You'll see a text field that says:
┌──────────────────────────────────────┐
│ Request Body: JSON                   │
│ ┌──────────────────────────────────┐ │
│ │ { }                              │ │
│ │ [cursor here]                    │ │
│ └──────────────────────────────────┘ │
└──────────────────────────────────────┘

16. Tap inside the text field where it says "{ }"


PART 6: Build the JSON (Using Variables, NOT typing text)
───────────────────────────────────────────────────────────────

You need to create this structure:
{
  "body": <Message Variable>,
  "sender": <Sender Variable>,
  "received_at": <Current Date Variable>
}

📝 HOW TO DO IT:

Step A: Type the opening brace and first key
────────────────────────────────────────────
Type exactly:
{
  "body":

(Don't forget the colon after "body")


Step B: Insert the Message Variable
────────────────────────────────────────────
Now DON'T TYPE "Shortcut Input" or "Message"!

Instead:
1. Tap in the space AFTER the colon "body": [cursor]
2. A popup menu appears at bottom of screen showing available variables
3. Look for "Shortcut Input" or "Message" (it's a blue rounded pill)
4. Tap it

You'll see a blue pill appear:
{
  "body": [Shortcut Input 📱]
           ↑ This is a blue variable pill, not typed text!


Step C: Add the sender field
────────────────────────────────────────────
Continue typing:
{
  "body": [Shortcut Input],
  "sender":

Then:
1. Tap after "sender": [cursor]
2. The variable menu appears again
3. Select "Sender" (another blue pill)

Result:
{
  "body": [Shortcut Input],
  "sender": [Sender]


Step D: Add the received_at field
────────────────────────────────────────────
Continue typing:
{
  "body": [Shortcut Input],
  "sender": [Sender],
  "received_at":

Then:
1. Tap after "received_at": [cursor]
2. Variable menu appears
3. Select "Current Date" (blue pill)

Final result:
{
  "body": [Shortcut Input],
  "sender": [Sender],
  "received_at": [Current Date]
}

Don't forget to type the closing brace: }


WHAT IT SHOULD LOOK LIKE
───────────────────────────────────────────────────────────────

The final Request Body field should have:
- Normal black text for: { } " , :
- Blue rounded pills for the three variables

Like this visual:
┌────────────────────────────────────────────┐
│ {                                          │
│   "body": [Shortcut Input]  ← blue pill   │
│   "sender": [Sender]        ← blue pill   │
│   "received_at": [Current Date] ← blue pill│
│ }                                          │
└────────────────────────────────────────────┘

✅ CORRECT: Blue pills for variables
❌ WRONG: If you see "Shortcut Input" in quotes as black text


PART 7: Save and Test
───────────────────────────────────────────────────────────────

17. Tap "Done" (top right)
18. Your automation is now active!

Test by:
- Sending yourself an SMS from the bank sender name
- Or wait for a real bank transaction


TROUBLESHOOTING
───────────────────────────────────────────────────────────────

Problem: "I don't see the variable menu when I tap"
Solution: Make sure you've added "Get Contents of URL" action first.
         Variables appear because the action is triggered by a Message.

Problem: "Variables showing as black text, not blue pills"
Solution: Delete the text and tap in that spot again. Select from
         the popup menu, don't type the variable name.

Problem: "Current Date not in the list"
Solution: Scroll down in the variable menu, or tap "Show More"

Problem: "JSON syntax error"
Solution: Check for:
         - Matching quotes around keys: "body" not body
         - Commas between fields (but NOT after last one)
         - Matching braces: { at start, } at end

═══════════════════════════════════════════════════════════════

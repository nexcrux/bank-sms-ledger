═══════════════════════════════════════════════════════════════
  🎯 UNDERSTANDING VARIABLES IN iOS SHORTCUTS
═══════════════════════════════════════════════════════════════

THE KEY CONCEPT: Variables are DYNAMIC placeholders
───────────────────────────────────────────────────────────────

When you SELECT a variable (blue pill), it gets replaced with real
data when the shortcut runs.

When you TYPE text, it's just static text.


EXAMPLE: What happens at runtime
───────────────────────────────────────────────────────────────

❌ WRONG WAY (Typing the text):
────────────────────────────────
You type in the JSON field:
{
  "body": "Shortcut Input",
  "sender": "Sender"
}

When automation runs, Cloudflare receives LITERALLY:
{
  "body": "Shortcut Input",     ← Just the text "Shortcut Input"!
  "sender": "Sender"             ← Just the text "Sender"!
}

This is useless! We want the actual SMS content, not those words.


✅ CORRECT WAY (Selecting variables):
──────────────────────────────────────
You type the structure, but INSERT variables:
{
  "body": [Shortcut Input],      ← Blue pill, not text
  "sender": [Sender]             ← Blue pill, not text
}

When automation runs with this SMS:
"Purchase of SAR 150 at Starbucks on 28/01/2024"
from sender "AlRajhiBank"

Cloudflare receives:
{
  "body": "Purchase of SAR 150 at Starbucks on 28/01/2024",
  "sender": "AlRajhiBank"
}

This is what we want!


VISUAL DIFFERENCE ON YOUR SCREEN
───────────────────────────────────────────────────────────────

WRONG - All black text:
┌────────────────────────────────────┐
│ {                                  │
│   "body": "Shortcut Input",        │  ← Black text in quotes
│   "sender": "Sender"               │  ← Black text in quotes
│ }                                  │
└────────────────────────────────────┘


CORRECT - Blue pills:
┌────────────────────────────────────┐
│ {                                  │
│   "body": ┌──────────────────┐    │  ← Blue rounded pill
│           │ Shortcut Input 📱│    │  ← Might have icon
│           └──────────────────┘    │
│   "sender": ┌────────┐            │  ← Blue rounded pill
│             │ Sender │            │
│             └────────┘            │
│ }                                  │
└────────────────────────────────────┘


HOW TO INSERT VARIABLES (Step-by-step)
───────────────────────────────────────────────────────────────

1. Tap in the JSON field where you want the variable
   Example: after typing "body": [tap here]

2. A menu pops up from the bottom showing available variables:
   ┌──────────────────────────────────────┐
   │ Shortcut Input                   ↑   │  ← The SMS message
   │ Sender                           ↑   │  ← The sender name/number
   │ Current Date                     ↑   │  ← Current timestamp
   │ Message                          ↑   │  ← Same as Shortcut Input
   └──────────────────────────────────────┘

3. Tap the variable you want (e.g., "Shortcut Input")

4. It appears as a blue pill in your JSON

5. Continue typing the rest (commas, next key, etc.)


WHAT EACH VARIABLE MEANS
───────────────────────────────────────────────────────────────

[Shortcut Input] or [Message]
   → The actual SMS text content
   → Example value: "Purchase of SAR 150 at Starbucks on 28/01/2024"

[Sender]
   → Who sent the SMS
   → Example value: "AlRajhiBank" or "+966501234567"

[Current Date]
   → Right now (when the shortcut runs)
   → Example value: "2024-01-28T14:30:00Z"


COMPLETE EXAMPLE
───────────────────────────────────────────────────────────────

You build this in Shortcuts:
┌────────────────────────────────────────────┐
│ {                                          │
│   "body": [Shortcut Input],                │
│   "sender": [Sender],                      │
│   "received_at": [Current Date]            │
│ }                                          │
└────────────────────────────────────────────┘

Bank SMS arrives:
   From: "AlRajhiBank"
   Text: "Dear Customer, SAR 200.00 debited from card 1234"

iPhone automatically runs shortcut and sends to Cloudflare:
{
  "body": "Dear Customer, SAR 200.00 debited from card 1234",
  "sender": "AlRajhiBank",
  "received_at": "2024-01-28T14:30:15Z"
}

Cloudflare saves it to database ✅


QUICK TEST: How to know if you did it right
───────────────────────────────────────────────────────────────

Look at your Request Body field:

✅ Good signs:
   - You see colorful rounded pills (usually blue/purple)
   - Pills have small icons or different background
   - You can tap a pill and it highlights
   - Tapping a pill shows a menu to change/remove it

❌ Bad signs:
   - Everything is black text
   - "Shortcut Input" appears in quote marks
   - No colorful pills visible
   - Looks like plain typed text


IF YOU MADE A MISTAKE
───────────────────────────────────────────────────────────────

To fix:
1. Tap and hold on the wrong text
2. Select and delete it
3. Tap in that spot again
4. Wait for variable menu to appear
5. Select the correct variable from the menu


STILL CONFUSED?
───────────────────────────────────────────────────────────────

Think of it like Mail Merge in Word:
- You don't type "Customer Name" in the letter
- You insert a [CUSTOMER_NAME] placeholder
- When printed, it gets replaced with actual names

Same concept here:
- Don't type "Shortcut Input"
- Insert the [Shortcut Input] variable
- When run, it gets replaced with actual SMS text

═══════════════════════════════════════════════════════════════
